O que é um objeto?
- é uma coisa material ou abstrata que pode ser percebida pelos sentidos e descrita por meio das suas caracteristicas, comportamentos e esado atual.

Classe -> Molde

o que o objeto tem- > atributo;
o que o objeto faz -> metodo;
como o objeto esta no momento -> Estado;

Instanciar é transformar uma classe em objeto

objeto = novo class;
caneta1 = new Caneta;
caneta1.cor = "Azul";
caneta1.rabiscar()

Classe:
Define os atributos e métodos comuns que serão compartilhados por um objeto;

Objeto:
é a instancia de uma classe;


Atividade:

Objeto : vaso
Atributo: cor, formato, tamanho;
metodo: semear, cultivar, propagar
estado: plantado.